# W12 SDD Progress

- Task 1: done
- baseline HEAD: c8bd2c9cc3a67804b4610b7ee0b2aed4a3e7dc87
- baseline status: `A  docs/superpowers/specs/2026-07-28-w12-bounded-render-time-detail-design.md`; `A  docs/superpowers/specs/2026-07-28-w12-handoff.md`; `?? docs/superpowers/plans/`
- baseline src/shaders patch: empty; SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; hunks: none
- files: `.superpowers/sdd/progress.md`; `docs/evidence/w12-bounded-render-time-detail/worktree-baseline.txt`; `docs/evidence/w12-bounded-render-time-detail/worktree-baseline.patch`; `openspec/changes/add-bounded-render-time-cloud-detail/**`
- verification: `openspec validate add-bounded-render-time-cloud-detail --strict --no-interactive` — exit 0
- changed paths: `.superpowers/sdd/progress.md`; `docs/evidence/w12-bounded-render-time-detail/worktree-baseline.txt`; `docs/evidence/w12-bounded-render-time-detail/worktree-baseline.patch`; `openspec/changes/add-bounded-render-time-cloud-detail/proposal.md`; `openspec/changes/add-bounded-render-time-cloud-detail/design.md`; `openspec/changes/add-bounded-render-time-cloud-detail/tasks.md`; `openspec/changes/add-bounded-render-time-cloud-detail/specs/cloud-rendering/spec.md`; `openspec/changes/add-bounded-render-time-cloud-detail/specs/cloud-params/spec.md`; `openspec/changes/add-bounded-render-time-cloud-detail/specs/cloud-detail/spec.md`

- Task 1 independent spec review: NEEDS_FIXES — `openspec validate add-bounded-render-time-cloud-detail --strict --no-interactive` exit 0; missing complete MODIFIED requirements for existing quality-mode and world-step default semantics.

- Task 1 independent re-review: NEEDS_FIXES — strict validate exit 0; `cloud-params` sets `detailFreq=0.5`, but the approved W12 design requires the fifth default to be `detailFreq=1.0`.

- Task 1 independent re-review: APPROVED — `detailFreq` defaults are 1.0 in the W12 change; GUI range remains 0.5–16.0; strict validate exit 0.
- Task 1 review fixes: done — `openspec validate add-bounded-render-time-cloud-detail --strict --no-interactive` exit 0; changed paths: `.superpowers/sdd/progress.md`; `openspec/changes/add-bounded-render-time-cloud-detail/specs/cloud-rendering/spec.md`; `openspec/changes/add-bounded-render-time-cloud-detail/specs/cloud-params/spec.md`; `openspec/changes/add-bounded-render-time-cloud-detail/specs/cloud-detail/spec.md`.
- Task 1 final spec correction: done — W12 `detailFreq` default corrected from 0.5 to 1.0; `openspec validate add-bounded-render-time-cloud-detail --strict --no-interactive` exit 0; changed paths: `.superpowers/sdd/progress.md`; `openspec/changes/add-bounded-render-time-cloud-detail/specs/cloud-params/spec.md`.

- Task 2: done; changed paths: `src/rendering/densityDetailResources.ts`; `scripts/check-w12-detail-contract.mjs`; `shaders/cloud.wgsl`; `src/rendering/densityQualityContracts.ts`; `src/rendering/densityQualityPipelines.ts`; `src/renderer.ts`; `.superpowers/sdd/progress.md`; RED: `node scripts/check-w12-detail-contract.mjs` exit 1 (missing `src/rendering/densityDetailResources.ts`); GREEN: `node scripts/check-w12-detail-contract.mjs` exit 0; verification: `npm run typecheck` exit 0.

- Task 2 independent re-review: APPROVED — dummy Base/Detail descriptors are 3D; the illegal-only ABI guard is statically reachable from both Hybrid adapters and every cloud/cloud-frame/ground-shadow entry, excluded from Cached/Realtime source manifests, and has no legal `enabled` sampling/output effect. `node scripts/check-w12-detail-contract.mjs` and `npm run typecheck` exit 0.

- Task 2 independent review: NEEDS_FIXES — P0 `src/renderer.ts:1435-1446` creates default 2D textures then 3D views; P0 `shaders/cloud.wgsl:164-167` is not statically used while auto-layout bind groups add 4–7 in `src/rendering/densityQualityPipelines.ts:232-254`. `node scripts/check-w12-detail-contract.mjs` and `npm run typecheck` exit 0 but do not detect either runtime validation failure.

- Task 2 review fixes: done; changed paths: `src/renderer.ts`; `shaders/cloud.wgsl`; `scripts/check-w12-detail-contract.mjs`; `.superpowers/sdd/progress.md`; RED: `node scripts/check-w12-detail-contract.mjs` exit 1 (dummy texture dimension, then missing Hybrid ABI reachability guard); GREEN: `node scripts/check-w12-detail-contract.mjs` exit 0; verification: `npm run typecheck` exit 0.

- Task 3: done; changed paths: `scripts/check-w12-sample-budget.mjs`; `shaders/cloud.wgsl`; `.superpowers/sdd/progress.md`; RED: `node scripts/check-w12-sample-budget.mjs` exit 1 (missing `DetailControls`); GREEN: `node scripts/check-w12-sample-budget.mjs` exit 0; verification: `node scripts/check-w12-sample-budget.mjs` exit 0.

- Task 3 review P1: done; changed paths: `scripts/check-w12-sample-budget.mjs`; `.superpowers/sdd/progress.md`; verification: `node scripts/check-w12-sample-budget.mjs` exit 0; `npm run typecheck` exit 0.

- Task 3 review adapter coverage: done; changed paths: `scripts/check-w12-sample-budget.mjs`; `.superpowers/sdd/progress.md`; verification: `node scripts/check-w12-sample-budget.mjs` exit 0; `npm run typecheck` exit 0.

- Task 3 independent review: NEEDS_FIXES — P1 `scripts/check-w12-sample-budget.mjs:38-63` leaves continuous mixing for four scalar controls, both `detailFreq` wavelength divisions, and the Task-3-only no-call boundary unconstrained; `node scripts/check-w12-sample-budget.mjs` and `npm run typecheck` exit 0.

- Task 3 independent re-review: NEEDS_FIXES — P1 `scripts/check-w12-sample-budget.mjs:69` scans only `shaders/cloud.wgsl`; it misses premature `sampleDetailField` calls from `src/rendering/densityShaderSources.ts` Hybrid adapters. Six-field mix, both `detailFreq` divisions, and Task 2 guard uniqueness are covered; `node scripts/check-w12-sample-budget.mjs` and `npm run typecheck` exit 0.

- Task 3 independent final re-review: APPROVED — the budget check reads `src/rendering/densityShaderSources.ts`, extracts both Hybrid adapter templates, and rejects both `sampleDetailField` and `applyBoundedDetailStage` before Task 4; `node scripts/check-w12-sample-budget.mjs` and `npm run typecheck` exit 0.

- Task 4: RED — `node scripts/check-w12-density-monotonic.mjs` exit 1 (missing `fn applyBoundedDetailStage(`); `npm run test:pipeline-isolation` exit 1 (missing source marker `fn applyBoundedDetailStage(`).

- Task 4: done; changed paths: `scripts/check-w12-density-monotonic.mjs`; `shaders/cloud.wgsl`; `src/rendering/densityShaderSources.ts`; `scripts/check-density-pipeline-isolation.mjs`; `scripts/check-w12-sample-budget.mjs`; `.superpowers/sdd/progress.md`; GREEN: `node scripts/check-w12-density-monotonic.mjs` exit 0; `npm run test:pipeline-isolation` exit 0; `node scripts/check-w12-sample-budget.mjs` exit 0.

- Task 4 reviewer P1: done; moved `DetailResourceControlsGPU` and group 3 bindings 4–7 from `shared-abi` into `hybrid-detail` after the stage definition; isolation now builds global/hierarchical Cached and Hybrid closures and verifies detail ABI ownership. Changed paths: `shaders/cloud.wgsl`; `scripts/check-density-pipeline-isolation.mjs`; `scripts/check-w12-detail-contract.mjs`; `scripts/check-w12-density-monotonic.mjs`; `scripts/check-w12-sample-budget.mjs`; `.superpowers/sdd/progress.md`; GREEN: `node scripts/check-w12-detail-contract.mjs` exit 0; `node scripts/check-w12-density-monotonic.mjs` exit 0; `npm run test:pipeline-isolation` exit 0; `node scripts/check-w12-sample-budget.mjs` exit 0.

- Task 4 independent review: NEEDS_FIXES — P1 `src/rendering/densityShaderSources.ts` 的 `shared-abi` 仍把 detail binding 4–7 拼入 Cached、Realtime、hierarchical Cached；`scripts/check-density-pipeline-isolation.mjs` 也未拼装/验证 hierarchical 源，无法证明 non-Hybrid 无 detail slice/ABI/atlas。`node scripts/check-w12-density-monotonic.mjs`、`node scripts/check-density-pipeline-isolation.mjs`、`node scripts/check-w12-sample-budget.mjs` exit 0；两个对应 npm 入口缺失，已改以这些真实脚本执行。

- Task 4 independent re-review: APPROVED — P1 closed: detail ABI is inside the Hybrid slice after its leading `applyBoundedDetailStage`; global/hierarchical Hybrid each have one ABI and one stage, while Cached/Realtime/hierarchical Cached have no detail ABI/stage/sample. `node scripts/check-w12-detail-contract.mjs`; `node scripts/check-w12-density-monotonic.mjs`; `node scripts/check-density-pipeline-isolation.mjs`; `node scripts/check-w12-sample-budget.mjs` exit 0.

- Task 5: RED — `node scripts/check-w12-light-rough.mjs` exit 1; `shaders/cloud.wgsl` has five one-argument real call sites: light, legacy ground, adaptive ground, main `densityAtTyped`, and silver probe. The cloud scalar wrapper plus all five TS adapter wrapper calls are already two-argument; no additional real call sites were found in the cloud shader or TS adapters.

- Task 5: done; changed paths: `scripts/check-w12-light-rough.mjs`; `shaders/cloud.wgsl`; `.superpowers/sdd/progress.md`. GREEN: `node scripts/check-w12-light-rough.mjs` exit 0 (enumerated 11 real cloud/TS-adapter calls; no one-argument calls); `npm run test:w10b-raymarch` exit 0; `npm run test:w10b-world-raymarch` exit 0. The stage `!wantFinal` return remains before `sampleDetailField`; global/hierarchical Hybrid forward `wantFinal`, and Cached/Realtime/hierarchical Cached remain bool-compatible edge paths.

- Task 5 reviewer P1 fixes: done; changed paths: `scripts/check-w12-light-rough.mjs`; `.superpowers/sdd/progress.md`. The check now requires exactly 11 real calls (cloud 6; each of five adapters 1), exact exclusive per-function rough/final call sets, exact Hybrid stage operands, and edge-only non-Hybrid adapters. GREEN: `node scripts/check-w12-light-rough.mjs` exit 0; `npm run test:w10b-raymarch` exit 0; `npm run test:w10b-world-raymarch` exit 0.

- Task 5 independent review: NEEDS_FIXES — P1 `scripts/check-w12-light-rough.mjs:75-85` does not assert the required exact 11-call set or exclusive rough/final role mapping; a new two-argument call or incorrect additional true/false call can pass. Current implementation manually verifies as correct: one main final; light, both ground paths, and silver rough; two Hybrid wrappers forward `wantFinal`; three non-Hybrid wrappers retain `applyEdgeShaping()` without stage; stage rough return precedes Base/Detail sampling. `node scripts/check-w12-light-rough.mjs`; `npm run test:w10b-raymarch`; `npm run test:w10b-world-raymarch` exit 0.

- Task 5 independent re-review: APPROVED — original P1 closed. `scripts/check-w12-light-rough.mjs` now requires exactly 11 real calls (cloud 6; five adapters one each), excludes definitions, and checks exact per-function call sets so main is uniquely final while light, legacy/adaptive ground, and silver are uniquely rough. Both Hybrid wrappers pass `sample…(pos), pos, wantFinal` exactly once; all three non-Hybrid adapters keep `applyEdgeShaping()` and contain no stage; rough returns before Base/Detail sampling. `node scripts/check-w12-light-rough.mjs`; `npm run test:w10b-raymarch`; `npm run test:w10b-world-raymarch` exit 0.

- Task 6: done; changed paths: `src/params.ts`; `src/i18n.ts`; `shaders/cloud.wgsl`; `src/renderer.ts`; `scripts/check-w12-light-rough.mjs`; `.superpowers/sdd/progress.md`. RED: `node scripts/check-w12-light-rough.mjs` exit 1 (missing W12 defaults). GREEN: `node scripts/check-w12-light-rough.mjs` exit 0; verification: `npm run typecheck` exit 0. Defaults are `true/120/512/1/1`; debug 18/19 reuse current-only `backgroundRadiance.a`, with a composite-after `sceneView` overlay and no history attachment or cloud-frame ABI change. The check now requires the one debug-19 rough call in addition to the Task-5 graph and constrains the debug carrier, target count, predicates, alpha isolation, sample scope, weights, and no-cloud values.

- Task 6 reviewer P1 fixes: done; changed paths: `src/gui.ts`; `src/i18n.ts`; `scripts/check-w12-light-rough.mjs`; `.superpowers/sdd/progress.md`. GUI now exposes W12 debug 18 erosion and 19 signed final-minus-rough with en/zh labels. The check parses the GUI option range, i18n dictionary entries, CloudFrameSample/targets/attachment array, composite sample member, TAA/TAAU/history blocks and bindings, W11/W12 overlay blocks, and exact predicate call counts. GREEN: `node scripts/check-w12-light-rough.mjs` exit 0; `npm run typecheck` exit 0.

- Task 6 independent review: NEEDS_FIXES — P1 `src/gui.ts:678-698` / `src/i18n.ts:190-209`: debug 18/19 have no selectable GUI options or i18n labels. P1 `scripts/check-w12-light-rough.mjs:136-161,179-196`: key Task-6 assertions use weak raw `includes`; e.g. composite RGB check does not reject an alpha read. Require function/source-block exact checks and negative assertions. Verified only `node scripts/check-w12-light-rough.mjs` and `npm run typecheck`, both exit 0.

- Task 6 independent re-review: APPROVED — original P1s closed. GUI preserves 16/17 and exposes localized 18/19. The checker uses source blocks, exact counts, and rejects composite `.a`/`.rgba`; it covers targets, attachments, temporal history/TAA/TAAU, overlay and predicates. `node scripts/check-w12-light-rough.mjs`; `npm run typecheck` exit 0.

- Task 7: done; changed paths: `scripts/check-w12-density-monotonic.mjs`; `package.json`; `.superpowers/sdd/progress.md`. Added real `test:w12-detail-contract`, `test:w12-density-monotonic`, `test:w12-sample-budget`, and `test:w12-light-rough` npm scripts. The monotonic check now protects the approved dilated-support remap, erosion-only low threshold, and bounded final remap. Verification: `npm run test:w12-detail-contract` exit 0; `npm run test:w12-density-monotonic` exit 0; `npm run test:w12-sample-budget` exit 0; `npm run test:w12-light-rough` exit 0; `npm run test:pipeline-isolation` exit 0; `npm run typecheck` exit 0.

- Task 7 reviewer P1 fixes: done; changed paths: `scripts/check-w12-density-monotonic.mjs`; `.superpowers/sdd/progress.md`. The checker now requires the exact rough-base/remap declaration and pre-sample rough return, then the exact erosion low-threshold and final remap over the same rough base; the existing `hi <= lo` zero guard protects the `lo >= 1` final case. Verification: `npm run test:w12-detail-contract` exit 0; `npm run test:w12-density-monotonic` exit 0; `npm run test:w12-sample-budget` exit 0; `npm run test:w12-light-rough` exit 0; `npm run test:pipeline-isolation` exit 0; `npm run typecheck` exit 0.

- Task 7 independent review: NEEDS_FIXES — P1 `scripts/check-w12-density-monotonic.mjs:65-72` uses raw substring checks and does not assert rough's actual derivation, so an implementation with `rough = 0.0` can pass while violating `final <= rough`; require structural executable-statement checks for rough/final/lo/remap. Package adds exactly four accurate W12 entries with no unrelated package changes. Six requested commands all exit 0.

- Task 8: added `capture:w12`, `evidence:w12-runtime`, and `gate:w12` with headless Chromium/Vite capture, fixed postprocess matrix, runtime cost collector, and owner-gated report. Commands: initial `npm run capture:w12` outer exit 124 while page readiness waited; rerun after phase logging/startup-failure capture exited 1 with actual environment failure `Cached density quality pipeline creation failed: cached-global-only-pipeline-create-failed: 1344:65 'final' is a reserved keyword` from `shaders/cloud.wgsl`, so zero matrix screenshots/runtime inputs were produced. `npm run evidence:w12-runtime` exit 0 (rerun after unavailable propagation, 0 pairs/1 unavailable); `npm run gate:w12` exit 0. Artifacts: `capture-index.json`, timestamped `capture-runs/*.json`, `runtime-evidence.json`, `gate-report.json`, `gate-w12.md`. Unavailable axes: all visual/runtime matrix and debug 18/19 due renderer startup; shadow timing freshness because the existing page diagnostics do not expose `shadowRan`/`shadowSampleId`/`shadowMs`; atlas-unavailable and Legacy fallback forcing because no existing controller API exposes those controls. `runtimeSourceMatchesHead=false`; Gate `REVIEW`, owner visual verdict `PENDING`.

- Task 8 runtime follow-up: after two WGSL startup P0 fixes, a third `npm run capture:w12` reached page readiness and matrix selection but produced 0/10 captures, then exited nonzero from ten recorded capture errors. Real WebGPU failure for each available Hybrid case: `density-quality-hybrid-ground-shadow-scene` bind group has 4 entries while its expected layout has 5 (missing binding 4 uniform, minBindingSize 1280); exact diagnostics are preserved in the third `capture-runs/*.json` and current `capture-index.json`. Task8 manifest finding: no matching Hybrid `global-only` or `hierarchical` Cu/Cb cases were present for four requested matrix rows. Collector/Gate intentionally not rerun after this failed capture; no business code changed.

- Task 8 final capture: script maps the approved matrix to existing manifest cases without changing business code: W9 global-only/hierarchical for storage coverage, W6 Recipe V2 Hybrid Cu, W8 Recipe V2 Hybrid Cb, and W9 for Sc/Ac/Cc/equal-overlap/far/thin-ridge. Added auditable `--resume`: validates same HEAD, `runtimeSourceMatchesHead`, and matrix SHA-256; retries only failed Ac with a fresh browser context, 300s readiness timeout, two-attempt ceiling; atomically merges capture index by stem and preserves attempt provenance. The initial complete 87-capture run had Ac readiness timeout only (not visual failure); `npm run capture:w12 -- --resume` completed Ac on fresh attempt 1, producing 95 merged captures and exit 0. Final `npm run evidence:w12-runtime` exit 0: 20 fixed full-res/TAAU pairs, all current timings available; fresh shadow timing unavailable for all pairs because the existing controller omits `shadowRan`/`shadowSampleId`/`shadowMs`; atlas-unavailable and Legacy fallback remain unavailable because no controller force API exists. Final `npm run gate:w12` exit 0: `REVIEW`, owner verdict `PENDING`, `runtimeSourceMatchesHead=false`; zero tolerance rows 8 UNABLE, required evidence 6 OBSERVATION/2 UNABLE (edge-only/wind-motion have no existing controllable interface). Artifacts updated: final `capture-index.json`, resume `capture-runs/*.json`, `runtime-evidence.json`, `gate-report.json`, `gate-w12.md`.

- Task 8 reviewer P1: no capture rerun and no business-code changes. Capture attempts now keep local `attemptResults`, expected eight variant stems, attempt provenance, and attempt-scoped artifacts; only a complete/unique set atomically replaces that case in `evidence.results`, while failed attempts remain out of the index. Resume validates HEAD/runtime source/matrix fingerprint, uses fresh-context attempts, and records failure separately. Collector now requires exactly one sample for every matrix×temporal×detail×skipLight slot; incomplete/duplicate sets reject timing and iteration calculations and set runtime integrity FAIL. Gate carries runtime integrity, preserving REVIEW for invalid evidence. Verification: `npm run evidence:w12-runtime` exit 0 (20 pairs, integrity PASS/0 issues); `npm run gate:w12` exit 0 (`REVIEW`, owner `PENDING`).

- Task 6 runtime P0 fix: done; changed paths: `shaders/cloud.wgsl`; `scripts/check-w12-light-rough.mjs`; `.superpowers/sdd/progress.md`. Renamed the reserved WGSL encoder parameter `final` to `finalDensity` without changing the signed `finalDensity - rough` mapping. The light/rough check now locks the exact encoder signature/expression and rejects the reserved identifier. Verification: `npm run test:w12-detail-contract`; `npm run test:w12-density-monotonic`; `npm run test:w12-sample-budget`; `npm run test:w12-light-rough`; `npm run test:pipeline-isolation`; `npm run typecheck` — all exit 0.

- Task 6 runtime P0 source-isolation fix: done; changed paths: `shaders/cloud.wgsl`; `src/rendering/densityShaderSources.ts`; `scripts/check-w12-light-rough.mjs`; `scripts/check-w12-sample-budget.mjs`; `scripts/check-density-pipeline-isolation.mjs`; `.superpowers/sdd/progress.md`. Shared `renderCloudFrame` now calls the uniform `w12DebugErosionAt(pos, typed)` adapter only from debug 18 and contains no Hybrid-only helper symbol. Global/hierarchical Hybrid adapters evaluate and sample once behind the effective-erosion guard; Cached, Realtime, and hierarchical Cached adapters return zero without detail helper/ABI/sample dependencies. Debug 19 retains only the rough density call. Verification: `npm run test:w12-detail-contract`; `npm run test:w12-density-monotonic`; `npm run test:w12-sample-budget`; `npm run test:w12-light-rough`; `npm run test:pipeline-isolation`; `npm run typecheck` — all exit 0.

- Task 8 runtime ground-shadow binding P0 fix: done; changed paths: `src/rendering/densityQualityPipelines.ts`; `scripts/check-w12-detail-contract.mjs`; `scripts/check-density-pipeline-isolation.mjs`; `.superpowers/sdd/progress.md`. Exact failure was Hybrid `groundShadowPipeline.getBindGroupLayout(0)` scene group binding 0 camera uniform (192 bytes), not binding 4 presets (1280 bytes, already present). Hybrid ground-shadow scene now binds camera 0 plus existing params/presets 1/4 and weather 2/3; Cached/Realtime mappings are unchanged. Hybrid detail remains a separate group 3 with bindings 4–7 and is absent from non-Hybrid source closures. RED: both enhanced checks exit 1 before the resource fix. GREEN: `npm run test:w12-detail-contract`; `npm run test:w12-density-monotonic`; `npm run test:w12-sample-budget`; `npm run test:w12-light-rough`; `npm run test:pipeline-isolation`; `npm run typecheck` — all exit 0. Capture was not rerun.

- Task 8 independent evidence review: NEEDS_FIXES — P1 `docs/evidence/w12-bounded-render-time-detail/run-capture-with-vite.mjs:157-160` writes each Ac variant directly to global results before the attempt completes; a mid-attempt failure leaves partial Ac data and a failed retry does not remove it. Buffer all eight variants locally and merge only after a successful attempt. Current artifacts are otherwise consistent: Ac prior failed before capture (0 partial), fresh same-HEAD/fingerprint resume completed 95 unique screenshots; 20 120/512 same-scene/mode pairs; shadow unavailable rather than zero; `runtimeSourceMatchesHead=false` keeps Gate `REVIEW`; zero/required lists are 8/8. `npm run evidence:w12-runtime` and `npm run gate:w12` exit 0.

- Task 8 independent re-review: APPROVED — original P1 is closed. Case attempt buffers exactly eight unique stems in its own attempt directory, replaces the complete case only after success, and failed attempts record `exposedToResults:false`; resume still requires same HEAD/runtime flag/matrix fingerprint and a fresh context. Collector exact-one validates every cost slot and rejects incomplete/duplicate variants; integrity propagates to Gate, which remains `REVIEW` with head=false/formal Continue=false/owner pending. `npm run evidence:w12-runtime`; `npm run gate:w12` exit 0; 20 pairs integrity PASS.

- Task 9 final-check W10B regression fix: done; changed paths: `scripts/check-w10b-raymarch-contracts.mjs`; `.superpowers/sdd/progress.md`. Updated only the stale default assertions to the approved W12 group `worldStepEnabled:true`, `worldStepMinMeters:120`, and `worldStepMaxIterations:512`; the W10B algorithm, override packing, and explicit `!worldMarch` fixed-step compatibility assertions remain intact. Verification: `npm run test:w10b-raymarch` exit 0; `npm run test:w10b-world-raymarch` exit 0; `npm run test:w12-detail-contract` exit 0; `npm run test:w12-density-monotonic` exit 0; `npm run test:w12-sample-budget` exit 0; `npm run test:w12-light-rough` exit 0; `npm run test:pipeline-isolation` exit 0; `npm run typecheck` exit 0.

- Task 9 closure: `worktree-baseline.txt` confirms the baseline `src`/`shaders` patch is empty, so every current unified=0 hunk in `shaders/cloud.wgsl`, `src/renderer.ts`, and `src/rendering/densityShaderSources.ts` is attributed per hunk to W12 Tasks 2–6 in `gate-w12.md`; staged handoff/design artifacts are excluded. Individually rerun: `openspec validate add-bounded-render-time-cloud-detail --strict --no-interactive`; `npm run test:w12-detail-contract`; `npm run test:w12-density-monotonic`; `npm run test:w12-sample-budget`; `npm run test:w12-light-rough`; `npm run test:pipeline-isolation`; `npm run test:w10b-raymarch`; `npm run test:w10b-world-raymarch`; `npm run typecheck` — all exit 0. OpenSpec marked only static-evidenced 2.1–3.4, 4.1, 5.1 complete. Remaining owner items: runtime evidence is 95 captures / 20 pairs but `runtimeSourceMatchesHead=false`; fresh shadow timing, atlas-unavailable, Legacy fallback, edge-only, and wind-motion are unavailable; eight zero-tolerance rows require owner visual verdict; Cb known deviation remains. Gate stays `REVIEW`, Formal Continue `NO`, owner `PENDING`; no commit/archive.

- W12 final independent two-phase review: **NEEDS_FIXES**. P1 implementation finding: `src/renderer.ts:1609-1614` still directly consumes `densityProducerSelector.getActive().getSharedFieldDiagnostics()` and only then wraps it with `createDensityDetailResources`; this violates approved design §5 and the `cloud-rendering` delta, which require renderer to stop directly linking/consuming producer diagnostics and receive the narrowed `DensityDetailResources` contract. Phase B otherwise confirms O1–O5, §1–§16/§17 mapping, task checkmarks (only 2.1–3.4/4.1/5.1), and no W13/W15/W16 scope extension. Required checks all exit 0: strict OpenSpec, four W12 checks, pipeline isolation, two W10B checks, typecheck, runtime evidence, and Gate. Runtime reports 20 pairs / 95 captures, 0 unavailable, but `runtimeSourceMatchesHead=false`; Gate remains `REVIEW`, Formal Continue `NO`, owner `PENDING`.

- W12 final review P1 producer-boundary fix: done. Changed paths: `src/density/densityProducerSelector.ts`; `src/renderer.ts`; `scripts/check-w12-detail-contract.mjs`; `docs/evidence/w12-bounded-render-time-detail/runtime-evidence.json`; `docs/evidence/w12-bounded-render-time-detail/gate-report.json`; `docs/evidence/w12-bounded-render-time-detail/gate-w12.md`; `.superpowers/sdd/progress.md`. `DensityProducerSelector.getActiveDetailResources(): DensityDetailResources` now owns the active producer diagnostics read and delegates narrowing to the existing `createDensityDetailResources` adapter injected by renderer; renderer consumes only the readonly narrow contract, including shared-field debug binding. The selector's rendering-contract dependency is `import type`, while the runtime adapter is injected from renderer, so no density/rendering runtime import cycle was introduced. The detail-contract check explicitly rejects the producer diagnostics getter in renderer and requires the selector narrow provider/type-only boundary. RED: `npm run test:w12-detail-contract` exit 1 (`renderer directly consumes producer diagnostics`). GREEN/final verification: `openspec validate add-bounded-render-time-cloud-detail --strict --no-interactive`; `npm run test:w12-detail-contract`; `npm run test:w12-density-monotonic`; `npm run test:w12-sample-budget`; `npm run test:w12-light-rough`; `npm run test:pipeline-isolation`; `npm run test:w10b-raymarch`; `npm run test:w10b-world-raymarch`; `npm run typecheck`; `npm run evidence:w12-runtime`; `npm run gate:w12` — all exit 0. Runtime collector reports 20 pairs / 0 unavailable with `runtimeSourceMatchesHead=false`; Gate remains `REVIEW`, owner `PENDING`. Capture and commit were not run.

- Task 8 Gate/Task9 preservation fix: changed only `docs/evidence/w12-bounded-render-time-detail/build-gate.mjs` and ledger. Gate builder reads the existing report, strictly accepts at most one correctly ordered `<!-- W12-TASK9-START -->` / `<!-- W12-TASK9-END -->` pair, preserves exactly that block verbatim, and otherwise appends an empty marker pair; no old automatic report content is retained. `npm run gate:w12` exit 0. Generated `gate-w12.md` has exactly one ordered marker pair; Gate remains `REVIEW`, owner `PENDING`. No Task9 content was filled and capture was not run.

- Task 9 final closure: reran separately with exit 0: strict OpenSpec; four W12 checks; pipeline isolation; two W10B checks; typecheck; `npm run evidence:w12-runtime`; `npm run gate:w12`. Collector: 20 pairs, integrity PASS, 0 unavailable entries, `runtimeSourceMatchesHead=false`; Gate: REVIEW, Formal Continue NO, owner PENDING. The stable Gate marker now contains exact unified=0 attribution for all 58 current W12 hunks (cloud 17, renderer 30, shader sources 11), including the four P1 selector/renderer hunks attributed to Task 2. OpenSpec statuses remain truthful: checked 2.1–3.4, 4.1, 5.1 only; 4.2/4.3/5.2/5.3 remain unchecked for stale runtime evidence and owner/unavailable axes. No commit/archive.

- W12 final independent re-review after producer-boundary and Gate-preservation fixes: **APPROVED_WITH_OWNER_BLOCKERS**. The prior P1 is closed: `DensityProducerSelector` type-imports `DensityDetailResources`, receives the narrowing adapter by injection, owns the active producer diagnostics read, and exposes only `getActiveDetailResources()` to renderer; renderer contains no `getSharedFieldDiagnostics`, and no runtime density/rendering import cycle or resource-contract leakage remains. The updated detail-contract check strengthens rather than weakens this boundary. The Task 9 marker has exactly one ordered pair, contains no old automatic report section or trailing injected content, and retained SHA-256 `c45203f39fa3f9b4607479d2578f6f62aeba336588d0dc571d4f3aa116b31a2e` across `gate:w12`; its three-file ledger matches all 58 current unified=0 headers exactly (17 cloud, 30 renderer, 11 shader-source). Strict OpenSpec, four W12 checks, pipeline isolation, two W10B checks, typecheck, runtime evidence, and Gate all exit 0. Tasks remain checked only through 2.1–3.4/4.1/5.1; 4.2/4.3/5.2/5.3 remain correctly unchecked. Owner blockers remain truthful: `runtimeSourceMatchesHead=false`, Gate `REVIEW`, Formal Continue `NO`, owner `PENDING`, fresh shadow/atlas-unavailable/Legacy/edge-only/wind-motion axes unavailable, and eight zero-tolerance visual verdicts pending. No capture/commit/archive.
